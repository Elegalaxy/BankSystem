#ifndef ACCOUNT_H
#define ACCOUNT_H
#include <string>
#include <map>
#include <vector>

class Account{
    public:
        std::string name, amount, accountNumber, password;
        Account();
};

#endif